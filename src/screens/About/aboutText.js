const aboutText = {
  stack: `React + Tailwind CSS on the frontend, AWS on the backend. Specifically, API Gateway to handle incoming requests,
    Lambda functions (in python + boto3) to handle the Lex chatbot and fetch from the database in DynamoDB.`,
  data: `Of course. The JSON data is available on the project's github, located`,
};

export default aboutText;
